#include <common.h>
#include <logger.h>
#include <args.h>
#include <cmd.h>

int	cmd_hash(const t_cmd *cmd)
{
	SSL_LOG(ERROR, NOT_IMPLEMENTED_ERROR);
	return (SSL_ERR);
}

int	cmd_des_ecb(const t_cmd *cmd)
{
	SSL_LOG(ERROR, NOT_IMPLEMENTED_ERROR);
	return (SSL_ERR);
}

int	cmd_des_cbc(const t_cmd *cmd)
{
	SSL_LOG(ERROR, NOT_IMPLEMENTED_ERROR);
	return (SSL_ERR);
}

int	cmd_rsa_gen(const t_cmd *cmd)
{
	SSL_LOG(ERROR, NOT_IMPLEMENTED_ERROR);
	return (SSL_ERR);
}

int	cmd_rsa(const t_cmd *cmd)
{
	SSL_LOG(ERROR, NOT_IMPLEMENTED_ERROR);
	return (SSL_ERR);
}

int	cmd_rsa_utl(const t_cmd *cmd)
{
	SSL_LOG(ERROR, NOT_IMPLEMENTED_ERROR);
	return (SSL_ERR);
}
